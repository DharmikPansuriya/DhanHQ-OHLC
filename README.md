# FastAPI Template

## Commands

`docker-compose -f local.yml up --build`

#### To run on Development Environment Use:

`docker-compose -f development.yml up --build`

#### To run on Production Environment Use:

`docker-compose -f production.yml up --build`
